#include <stdio.h>
void check_even_odd(int n) {
    if (n % 2 == 0) {
        printf("%d is even.\n", n);  
    } else {
        printf("%d is odd.\n", n); 
    }
}

int main() {
    int num;
    printf("Enter an integer: ");
    scanf("%d", &num);
    check_even_odd(num);

    return 0;
}
